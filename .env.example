# RecipeForADisaster Environment Configuration
# Copy this file to .env and update the values for your environment

# Azure OpenAI Configuration (for AI recipe generation)
# Get these values from your Azure OpenAI resource in the Azure portal
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/
AZURE_OPENAI_KEY=your-api-key-here
AZURE_OPENAI_DEPLOYMENT=gpt-35-turbo  # or gpt-4, gpt-4-turbo, etc.

# HashiCorp Vault Configuration (for secure credential management)
# Optional: If configured, credentials will be retrieved from Vault instead of environment variables
# For development, use the local Vault dev server:
VAULT_ADDR=http://127.0.0.1:8200
VAULT_TOKEN=root

# Application Configuration
APP_NAME=RecipeForADisaster
APP_VERSION=2.4.0

# Web server configuration
WEB_PORT=8080
WEB_HOST=0.0.0.0

# Database Configuration
# Database file paths - will default to ~/.recipeforadisaster/ if not set
RECIPES_DB_PATH=/app/data/recipes.db
USERS_DB_PATH=/app/data/users.db

# JWT Authentication Configuration
# CRITICAL: Change this to a secure random string in production!
JWT_SECRET=your-super-secure-jwt-secret-key-change-this-in-production
JWT_ISSUER=RecipeForADisaster
JWT_AUDIENCE=RecipeForADisaster-API
JWT_EXPIRATION_SECONDS=86400  # 24 hours in seconds

# Production Environment Settings
NODE_ENV=production
REACT_APP_API_URL=http://localhost:8080/api