# RecipeForADisaster Environment Configuration
# Copy this file to .env and update the values for your environment

# MongoDB Configuration
# For local development (after running setup-dev.sh):
MONGODB_URI=mongodb://localhost:27017

# For MongoDB Atlas (production):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/RecipeManagerDB

# For local MongoDB with authentication:
# MONGODB_URI=mongodb://admin:password@localhost:27017/RecipeManagerDB

# Azure OpenAI Configuration (for AI recipe generation)
# Get these values from your Azure OpenAI resource in the Azure portal
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/
AZURE_OPENAI_KEY=your-api-key-here
AZURE_OPENAI_DEPLOYMENT=gpt-35-turbo  # or gpt-4, gpt-4-turbo, etc.

# HashiCorp Vault Configuration (for secure credential management)
# Optional: If configured, credentials will be retrieved from Vault instead of environment variables
# VAULT_ADDR=https://your-vault-server.com:8200
# VAULT_TOKEN=your-vault-token-here

# Application Configuration
APP_NAME=RecipeForADisaster
APP_VERSION=1.0.0

# Optional: MongoDB connection pool settings
MONGODB_POOL_SIZE=10

# Optional: Web server configuration (for future web interface)
WEB_PORT=8080
WEB_HOST=localhost